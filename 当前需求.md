### 优化cybacktrader的性能
 
 #### 当前需求说明（v1.0）
 
 本需求旨在系统性优化 `cybacktrader` 的 Cython 实现以显著提升运行性能，同时保持对外 API 兼容与功能正确性。该文档与 `OPTIMIZATION_PLAN.md`、`benchmarks/README.md` 配套使用，作为执行与验收依据。
 
 ---
 
 #### 一、背景与目标
 
 - **背景**：`cybacktrader` 已完成对 backtrader 的 Cython 化重构，但多数 `.pyx` 仍基本等同于 Python 版本，尚未进行深入的类型与内存访问优化。
 - **目标**：在保持功能与 API 兼容的前提下，分阶段优化核心热路径，累计实现相对 backtrader **2-5 倍**的性能提升（详见 `OPTIMIZATION_PLAN.md` 的阶段目标）。
 
 ---
 
 #### 二、工作范围（Scope）
 
 - **在范围**：
   - 为热点与核心模块 `.pyx` 添加静态类型、使用内存视图、减少 Python 对象与函数调用开销、必要的内联与 C 标准库调用。
   - 针对主循环与频繁调用路径进行算法与数据访问优化。
   - 每个文件优化后进行编译、测试与基准验证，记录性能数据。
 - **不在范围**：
   - 对外部 API 的破坏性变更。
   - 非性能相关的新特性开发与大规模架构重写。
   - 移除或弱化现有测试用例与基准测试。
 
 ---
 
 #### 三、优化优先级与批次
 
 优先按照热点程度与调用频率推进（文件路径均在 `cybacktrader/` 下）：
 
 - **P0（最高优先）**：
   - `linebuffer.pyx`
   - `indicator.pyx`
   - `lineseries.pyx`
   - `strategy.pyx`
 
 - **P1（高优先）**：
   - `cerebro.pyx`
   - `broker.pyx`
   - `order.pyx`
   - `comminfo.pyx`
 
 - **P2（中优先）**：
   - `functions.pyx`
   - `metabase.pyx`
   - 常用指标与子模块（存在性以仓库为准，按需择优纳入）
 
 - **P3（低优先）**：
   - `utils/*`, `analyzers/*`, `observers/*` 等辅助模块
 
 注：具体清单与顺序以 `OPTIMIZATION_PLAN.md` 的矩阵为主，并结合实际仓库文件存在性动态调整。
 
 ---
 
 #### 四、执行流程与规范（每个文件）
 
 1. **预分析**：
    - 使用 Cython 注解与 profiler 定位热点：
      ```bash
      cython -a path/to/file.pyx
      python -m cProfile script.py  # 或基准脚本
      ```
 2. **实施优化**（示例手段）：
    - 添加 `cdef` 类型与函数签名，声明循环变量类型。
    - 使用内存视图（如 `double[:]`）替代 Python 层索引。
    - 减少 Python 对象分配与字典/列表频繁操作；必要时引入轻量 Cython cdef class/struct。
    - 内联小函数（`cdef inline`），使用 `libc.math` 等 C 库。
 3. **编译与测试**：
    ```bash
    pip install -U .
    pytest tests -n 8
    ```
 4. **基准验证与记录**：
    ```bash
    python benchmarks/ma_crossover_benchmark.py
    ```
    - 记录运行时间、加速比、数据规模；保存 Cython 注解 HTML（如适用）。
 5. **提交**：
    - 提交包含：变更摘要、优化点说明、性能对比数据与图表快照（如有）。
 
 ---
 
 #### 五、里程碑与计划（概要）
 
 - **阶段一（P0）**：完成 `linebuffer.pyx`、`indicator.pyx`、`lineseries.pyx`、`strategy.pyx` 的优化与验证。
 - **阶段二（P1）**：完成 `cerebro.pyx`、`broker.pyx`、`order.pyx`、`comminfo.pyx` 的优化与验证。
 - **阶段三（P2）**：补充 `functions.pyx`、`metabase.pyx` 及常用子模块。
 - **阶段四（P3）**：其余辅助模块按需覆盖。
 - 每阶段结束运行完整基准，评估累计加速比并调整后续计划（详见 `OPTIMIZATION_PLAN.md`）。
 
 ---
 
 #### 六、验收标准（Acceptance Criteria）
 
 - **功能正确性**：`pytest tests -n 8` 全量通过，且无新增回归。
 - **性能达标**：
   - 单阶段达成 `OPTIMIZATION_PLAN.md` 中对应阶段目标区间；
   - 累计加速比逐步向 2-5x 收敛；
   - 基准脚本在至少两档数据规模（如 1万、10万或更大）验证通过并记录。
 - **兼容性**：对外 API 不变，公共行为与语义保持一致。
 - **可维护性**：关键优化处有简要注释，避免过度微优化导致可读性极差。
 
 ---
 
 #### 七、交付物（Deliverables）
 
 - 优化后的 `.pyx` 代码与编译产物。
 - 基准测试结果：运行时间、加速比表格与图表（如 `ma_crossover_benchmark_results.png`）。
 - Cython 注解 HTML（如用于说明瓶颈与改进效果）。
 - 变更说明（概要列出修改点与收益）。
 
 ---
 
 #### 八、参考与附录
 
 - `OPTIMIZATION_PLAN.md`：总体架构、优先级矩阵、阶段目标与执行要点。
 - `benchmarks/README.md`：基准脚本说明与使用方法。
 - 样例命令：
   ```bash
   pip install -U . && pytest tests -n 8 && python benchmarks/ma_crossover_benchmark.py
   ```
 
 ---
 
 本文档为执行纲领，实际推进中如与仓库文件现状存在差异（例如某些子模块文件不存在），以仓库实际为准动态调整清单与排序，但必须遵守上述流程与验收标准。