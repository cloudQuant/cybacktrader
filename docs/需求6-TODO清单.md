# 需求6执行清单

## 当前状态：需求分析与策略制定完成

✅ 已完成：
- Cython文档学习（def vs cdef vs cpdef）
- 代码库扫描与分析（63个pyx文件，500+个def方法）
- 重构优先级清单制定
- 风险评估与保守策略确认

## 核心决策：采用保守方案B

**方案B：创建cdef辅助函数（零API破坏风险）**

不直接修改公开API，而是为热路径方法创建内部cdef实现。

## 阶段1：示例实施与验证（第1-2天）

### 目标：在1个文件上验证方案可行性

#### 文件：position.pyx（最简单，9个方法）
- [ ] 为`update()`创建`_calc_update()`辅助函数
- [ ] 为`set()`创建`_calc_set()`辅助函数  
- [ ] 编译测试
- [ ] 运行完整测试套件
- [ ] 性能基准对比

**验收标准：**
- 测试全部通过
- 性能提升≥1%或持平
- 代码可读性不降低

如果失败→停止并重新评估
如果成功→进入阶段2

## 阶段2：扩展到核心文件（第3-5天）

### 文件2：trade.pyx（12个方法）
- [ ] 创建3-5个cdef辅助函数
- [ ] 测试与验证

### 文件3：linebuffer.pyx（74个方法）  
- [ ] 创建8-10个cdef辅助函数（最热路径）
- [ ] 重点优化：`__getitem__`, `__setitem__`, `forward`, `backwards`
- [ ] 测试与验证

### 文件4：lineseries.pyx（55个方法）
- [ ] 创建5-8个cdef辅助函数
- [ ] 测试与验证

**阶段2验收标准：**
- 所有测试通过
- 累计性能提升≥3-5%
- 无兼容性问题

## 阶段3：可选扩展（第6-7天，视阶段2效果决定）

仅当阶段2成功且收益明显时执行：

### 谨慎转换@cython.final方法为cpdef

**严格限制**：仅修改已标记`@cython.final`且简单的方法

候选方法（需逐个验证）：
- [ ] `Position.__len__()` 
- [ ] `Position.__bool__()`
- [ ] `Trade.__len__()`
- [ ] `Position.clone()`
- [ ] `Position.pseudoupdate()`

**每个方法的流程：**
1. 修改为cpdef
2. 编译
3. 运行测试
4. 如果失败→回滚→跳过该方法
5. 如果成功→保留→继续下一个

**阶段3退出条件：**
- 任何测试失败2次以上→停止阶段3
- 性能提升<1%→停止阶段3  
- 发现兼容性问题→回滚所有阶段3修改

## 阶段4：最终验证与文档（第8天）

### 完整回归测试
```bash
# 完整测试套件
pytest tests -n 8

# 性能基准（3轮）
python benchmarks/ma_crossover_benchmark.py --data-sizes 10000 100000 --rounds 3

# 内存测试
python benchmarks/ma_crossover_benchmark.py --data-sizes 100000 --rounds 1
```

### 文档输出
- [ ] 需求6完成报告（包含性能数据）
- [ ] 修改文件清单与代码diff摘要
- [ ] 经验教训与后续建议

## 风险控制措施

### 持续监控指标
- 每次修改后立即运行：`pytest tests -n 8`
- 记录每个阶段的性能数据
- 保持git清晰的commit历史（每个文件1个commit）

### 回滚策略
```bash
# 单文件回滚
git checkout -- cybacktrader/<file>.pyx

# 阶段回滚
git revert <commit-range>

# 完全回滚
git reset --hard <before-需求6-commit>
```

### 最坏情况应对
如果整个需求6的努力：
- 性能提升<2%
- 或导致任何兼容性问题

则：
- 完全回滚所有修改
- 以需求5的成果作为最终交付
- 在报告中说明：进一步优化的边际收益过低，风险较高

## 当前建议：开始执行阶段1

**下一步行动：**
1. 读取position.pyx完整内容
2. 设计2-3个cdef辅助函数
3. 实施修改
4. 编译测试
5. 评估结果，决定是否继续

## 预期总体收益（保守估计）

假设全部阶段成功：
- 阶段1（position.pyx）：+0.5-1%
- 阶段2（核心3个文件）：+2-4%
- 阶段3（cpdef转换）：+1-2%
- **总计：3-7%性能提升**

加上需求5的成果，应可满足"5-10x性能提升"的目标。

## 时间投入估算

- 阶段1：4-6小时
- 阶段2：8-12小时
- 阶段3：4-8小时（可选）
- 阶段4：2-4小时
- **总计：18-30小时**

## 决策点

**现在需要决定：**
1. 是否开始执行阶段1？
2. 还是认为需求5已足够，需求6风险过高？

**我的建议：**
至少尝试阶段1，因为：
- 风险极低（仅1个小文件）
- 可以验证方案可行性
- 即使失败，损失的时间成本很小
- 如果成功，有机会获得额外的性能提升
