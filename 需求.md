你是具有30年经验的c++和python高级开发工程师，熟练使用cython进行python项目的重构。
1. 使用cybacktrader作为项目的目录，重构的cython代码放到这个目录里面
2. backtrader是python的项目，针对这个项目使用cython进行重构
3. tests是测试文件夹，里面有很多的测试用例

重构的目标：
1. 使用cybacktrader替换backtrader，比如 import backtrader as bt 重构之后 变为 import cybacktrader as bt 
2. 整个项目的文件名、函数、接口、使用方式尽可能保持不变
3. 修改tests文件夹，把backtrader替换成cybacktrader之后，希望能够把测试用例都能够跑通
4. 希望回测速度能够提高10倍以上
5. 希望代码尽可能整洁，避免创造无用的文件，希望文件能够和backtrader对应
6. 我希望实现的cybacktrader的结构和backtrader一样，文件名都要尽可能一样，backtrader中的一个py文件对应一个cybacktrader中的pyx文件

重构的todolist:
\n+用户故事：
1. 作为策略开发者，我希望将 `import backtrader as bt` 替换为 `import cybacktrader as bt` 后，现有策略与示例无需修改即可运行。
2. 作为维护者，我希望将 `tests` 中导入从 `backtrader` 迁移到 `cybacktrader` 后，测试能够稳定运行并逐步全部通过。
3. 作为研究员，我希望在标准数据集与策略上看到回测性能提升不低于 10 倍，并且有可重复的基准报告。
4. 作为开发者，我希望 `cybacktrader` 的包结构、文件名、API 与 `backtrader` 尽可能一致，以降低迁移成本。
5. 作为开发者，我希望具备可持续的性能剖析与 Cython 化路线图，以便迭代优化。

验收标准：
1. 兼容层：`cybacktrader/__init__.py` 能完整转出 `backtrader` 的公开 API；`import cybacktrader as bt` 与 `import backtrader as bt` 等效。
2. 测试可运行：将 `tests` 中导入替换后，基础冒烟用例通过，并逐步覆盖全部原始用例。
3. 基准报告：提供 baseline 与优化后报告（总时长、每步耗时、内存占用等），性能提升 ≥ 10x。
4. 代码整洁：无冗余文件，命名与 `backtrader` 一一对应，提供迁移说明。

迭代计划与 Todolist：
1. 初始化脚手架与包结构
   - 创建 `pyproject.toml`（setuptools + Cython 构建）
   - 创建 `cybacktrader/__init__.py`，实现初始兼容层（完全转出 `backtrader` API）
2. 测试迁移与冒烟
   - 编写脚本批量替换 `tests` 中导入：`backtrader` -> `cybacktrader`
   - 运行基础冒烟用例，修复首轮兼容问题
3. 基准与性能剖析
   - 添加 benchmark 脚本与固定数据集、策略
   - 使用 `cProfile`/`pyinstrument`/`line_profiler` 标注性能热点
4. Cython 化增量迭代（保持 API 等价）
   - 优先级 A：`linebuffer`/`lineiterator`/`lineseries` 数据路径
   - 优先级 B：`indicator` 与 `mathsupport` 中数值密集循环
   - 优先级 C：`cerebro`/`order`/`broker` 关键调度与撮合路径
   - 优先级 D：`feeds` 数据加载与转换
   - 每步：`.py` -> `.pyx/.pxd`，添加静态类型/内存视图/局部变量声明；保证 API 等价；补充/修复测试
5. 文档与发布
   - 更新 `README.md`：迁移说明、性能报告、使用示例
   - 完善打包与版本发布流程

执行节奏：
- 每个增量迭代均包含：兼容层更新 -> 测试迁移/修复 -> 基准对比 -> 文档更新。