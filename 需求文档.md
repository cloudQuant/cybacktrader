
# cybacktrader 需求文档

## 1. 背景
你是一名具有丰富工程经验的资深工程师，熟悉 Python/C/C++，擅长使用 Cython 对 Python 项目进行性能优化与内存优化。`cybacktrader` 是对知名回测框架 `backtrader` 的 Cython 加速与兼容实现，目标是在保持 100% API 兼容的前提下，显著提升回测性能并降低内存占用。

参考仓库文件：`README.md`、`benchmarks/ma_crossover_benchmark.py`、`examples/simple_strategy.py`、`docs/OPTIMIZATION_SUMMARY.md`、`setup.py`、`pyproject.toml`。


## 2. 目标与范围
- **功能范围**：保持与 `backtrader` 100% API 兼容，可直接将 `import backtrader as bt` 替换为 `import cybacktrader as bt` 运行原有策略（参考 `examples/simple_strategy.py`）。
- **性能目标（最低验收线）**：
  - 回测耗时相对 `backtrader` 提升至少 5–10 倍（数据规模：1万–10万行，基准脚本见 `benchmarks/ma_crossover_benchmark.py`，每规模不少于 3 轮）。
  - 内存增量（RSS）相对 `backtrader` 降低 ≥ 50%。
- **兼容性目标**：Python ≥ 3.8；Linux/Mac/Windows 三平台可编译运行；保持与 `backtrader` 一致的行为与结果。
- **稳定性与可维护性**：
  - 通过现有单测与示例回归（`tests/`）。
  - 提供可复现实验的基准脚本与报告（基于 `benchmarks/`）。
- **非范围（Out of Scope）**：
  - 不引入破坏性 API 变更；不新增与 `backtrader` 无关的大功能。
  - 暂不引入 GPU/分布式等重型依赖；C/C++ 仅在必要时通过 Cython 封装使用。


## 3. 现状研判（来自仓库与文档）
- `docs/OPTIMIZATION_SUMMARY.md` 报告显示：核心引擎与指标等模块已进行大范围 Cython 化与深度优化，宣称整体完成度约 98%，在特定场景下可达 15–30x 的整体加速、30–50% 的内存下降。
- `setup.py` 中定义了详细的分层编译顺序（`LEVEL_1` 至 `LEVEL_7`），并收集 `cybacktrader/**/*.pyx` 进入构建；`pyproject.toml` 声明构建依赖。
- `benchmarks/ma_crossover_benchmark.py` 提供与 `backtrader` 的直接对比，包含耗时与内存增量统计（基于 `psutil`）。

注：上述“完成度/提升倍数”为文档与脚本层面的现状描述，最终以本需求文档的“验收标准”中可复现的实测结果为准。


## 4. 技术方案（Cython 优化策略）
- **静态类型与内联**：关键路径采用 `cdef`/`ctypedef`、`cdef inline`；在热点处内联消除函数调用开销。
- **Typed Memoryview**：对数组/序列使用 typed memoryview 实现零拷贝高效访问。
- **GIL 管理**：在纯计算环节使用 `with nogil:` 提升并行能力（需确保线程安全与数据独立）。
- **编译指令**：在安全的环境下，针对热点关闭边界检查、负索引与 wraparound（开发调试阶段可开启以保障稳定）。
- **数学库直连**：必要时直接使用 `libc.math` 等 C 级函数。
- **模块优先级与边界**：
  - P0：核心数据路径（`linebuffer.pyx`、`lineseries.pyx`、`functions.pyx`、`dataseries.pyx` 等）。
  - P1：指标（`indicator.pyx`、`indicators/*`，通过共享的基础算子实现“一处优化，全局受益”）。
  - P2：撮合/调度与外围（`broker.pyx`、`feed.pyx`、`observer.pyx`、`order.pyx`、`trade.pyx` 等）。
  - 特殊：含复杂元类或动态行为的模块需谨慎（例如 `strategy`/`metabase` 一类），必要时保留 Python 实现或采取“薄封装+厚基础”的优化路径。


## 5. 架构与模块边界（与构建顺序对应）
参照 `setup.py`：
- Level 1：通用工具和基础设施（`mathsupport.pyx`、`utils/*.pyx` 等）。
- Level 2：`metabase.pyx`、`lineroot.pyx`。
- Level 3：`linebuffer.pyx`、`functions.pyx`。
- Level 4：`lineseries.pyx`、`dataseries.pyx`、`lineiterator.pyx`。
- Level 5：交易与分析（`order.pyx`、`trade.pyx`、`position.pyx`、`comminfo.pyx`、`indicator.pyx`、`analyzer.pyx`、`observer.pyx`）。
- Level 6：执行与运行时（`feed.pyx`、`broker.pyx`、`strategy.pyx`、`signal.pyx`、`store.pyx`、`sizer.pyx`、`writer.pyx`、`timer.pyx` 等）。
- Level 7：`cerebro.pyx`（高度聚合，依赖广）。


## 6. 验收标准（Measurable Acceptance Criteria）
- **编译与运行**：
  - 编译安装： pip install -U .
  - 单元测试： pytests tests -n 8   （无异常崩溃；无明显内存泄漏（基于多轮运行的 RSS 观测应趋稳
- **功能兼容**：
  - 与 `backtrader` 在相同数据与参数下，关键统计和交易行为一致（如指标输出、成交、持仓与资金曲线）。
- **性能基线（以 `benchmarks/ma_crossover_benchmark.py` 为准）**：
  - 数据规模：至少包含 10,000 与 100,000 行；每规模运行不少于 3 轮。
  - 指标一：`avg(backtrader_time) / avg(cybacktrader_time) ≥ 5.0`。
  - 指标二：`memory_avg(cybacktrader) ≤ 50% * memory_avg(backtrader)`。
  - 若在更大规模（如 1,000,000 行）执行，结果不作硬性验收要求，仅作观察与报告。



## 7. 实施步骤（建议流程）
- **步骤 A：环境与构建**
  - 确认 Python、C 编译器、`Cython>=3.0`、`numpy` 已安装（参考 `pyproject.toml`）。
  - 本地构建：`python setup.py build_ext --inplace`。
- **步骤 B：基线与回归**
  - 跑 `benchmarks/ma_crossover_benchmark.py --data-sizes 10000 100000 --rounds 3` 产出基线报告。
- **步骤 C：热点与优化（如未达标）**
  - 使用 `benchmarks/compare_profiling_modes.py` 或其他分析手段定位热点差异。
  - 在 P0/P1 路径应用更激进的编译指令或算法优化（滚动窗口 O(n)、内联 C 循环、合理使用 `nogil` 等）。
- **步骤 D：反复验证**
  - 每次优化后重复步骤 B，记录耗时与内存指标，形成对比表/图。


## 8. 里程碑与交付物
- **M0：现状确认与复现实验（1–2 天）**
  - 交付物：基线构建日志、示例策略运行记录、首次基准数据（10K/100K）。
- **M1：性能达标（1–2 周，迭代式）**
  - 交付物：对比表与图（backtrader vs cybacktrader，含时间与内存）、关键优化点清单、Patch 列表。
- **M2：稳定性与回归（1 周）**
  - 交付物：测试报告（`tests/` 覆盖结果）、大规模数据的可用性观察报告（可选 1M 行）。
- **M3：文档与发布（1–2 天）**
  - 交付物：最终性能与验收报告、使用说明更新（如 `README.md`/`docs/`）。


## 9. 风险与应对
- **元类/动态行为导致的 Cython 不兼容**：
  - 应对：保留 Python 实现或采用“薄封装+厚基础”模式；在 `setup.py` 中跳过高风险模块，分层优化。
- **越界/负索引/空指针导致崩溃**：
  - 应对：开发期打开安全指令（`boundscheck`/`wraparound`/`nonecheck`），发布前在热点处定点关闭并做回归。
- **`nogil` 并行引发数据竞争**：
  - 应对：只在纯计算、无共享可变状态的代码段释放 GIL；必要时使用局部拷贝或原子/锁。
- **跨平台构建差异（尤其 Windows 工具链）**：
  - 应对：在 CI 或多平台环境复现构建；文档明确编译器要求与安装指引。


## 10. 环境与依赖
- Python ≥ 3.8，`Cython >= 3.0`，`numpy`。
- 需安装 C/C++ 编译器（Linux: gcc/clang；Windows: MSVC Build Tools；macOS: Xcode Command Line Tools）。
- 构建脚本：`setup.py`（分层收敛 `cybacktrader/**/*.pyx`）。


## 11. 验收与交付清单
- 编译产物：成功生成并可导入的扩展模块（`*.so`/`*.pyd`）。
- 兼容性：示例策略与基础测试通过，无功能回归。
- 性能报告：基于 `benchmarks/ma_crossover_benchmark.py` 的时间与内存对比，满足“5–10x 加速、内存 -50%”的最低验收线。
- 文档：本需求文档、执行记录、优化点说明、后续维护建议。


## 12. 附：运行与验证示例
- 构建：
```bash
python setup.py build_ext --inplace
```
- 示例策略：
```bash
python examples/simple_strategy.py
```
- 基准测试（建议 10K/100K、各 3 轮）：
```bash
python benchmarks/ma_crossover_benchmark.py --data-sizes 10000 100000 --rounds 3
```


## 13. 变更记录
- v1.0（本次）：重写并完善需求文档，加入可执行的验收标准、里程碑与风险清单；与仓库脚本和结构对齐。


## 14. 整体优化计划（Plan）

- **[阶段A] 文档与工具准备（0.5–1天）**
  - 研读 `docs/cython_docs/en/` 与 Cython 官方教程，结合 `docs/OPTIMIZATION_SUMMARY.md` 总结当前可行与不可行的模式（如元类限制）。
  - 熟悉并准备辅助脚本：`scripts/analyze_dependencies.py`、`scripts/check_compilation_status.py`、`scripts/compile_in_batches.py`、`scripts/monitor_compilation.py`、`scripts/compile_remaining.py`。
  - 确认本地/CI 的编译工具链与 Python 版本矩阵（Linux、macOS、Windows）。

- **[阶段B] 构建配置与模式切换（0.5–1天）**
  - 约定构建模式：Debug（安全指令全开）与 Release（热点定点关闭 `boundscheck`/`wraparound`/`nonecheck`、开启 `cdivision`）。
  - 验证 `setup.py` 分层构建顺序（Level 1–7）在两种模式下可稳定编译；必要时以批量脚本分批构建与回退。

- **[阶段C] 基线测评与目标差距分析（1–2天）**
  - 运行 `benchmarks/ma_crossover_benchmark.py --data-sizes 10000 100000 --rounds 3`，收集 backtrader 与 cybacktrader 的耗时与内存数据，形成对比表与可视化。
  - 若未达到“≥5x 加速、内存≥50%下降”，定位差距所在模块，进入阶段D/E。

- **[阶段D] P0 核心数据路径深度优化（1–2周）**
  - 面向 `cybacktrader/linebuffer.pyx`、`lineseries.pyx`、`functions.pyx`、`dataseries.pyx`、`lineiterator.pyx` 的循环与数据访问进行 C 级化处理：typed memoryview、`cdef inline` 内联、`with nogil`、消除 Python 调用与装箱开销、滚动/环形缓冲 O(1) 算法、`libc.math` 直连等。
  - 调整 Release 指令至热点路径，确保回归后稳定。

- **[阶段E] P1 指标体系“一处优化，全局收益”（1周）**
  - 以 `cybacktrader/indicators/basicops.pyx` 为优化中心，复用加速后的基础算子（SMA/EMA/Highest/Lowest/SumN/MeanDev等），系统性检查 10 大高频指标：`sma.pyx`、`ema.pyx`、`rsi.pyx`、`macd.pyx`、`bollinger.pyx`、`atr.pyx`、`stochastic.pyx`、`cci.pyx`、`momentum.pyx`、`williams.pyx`。
  - 校验 `once`/`next` 的实现是否已经 C 级循环，避免 Python 层逐bar调用。

- **[阶段F] P2 调度/撮合/外围模块收尾（0.5–1周）**
  - `broker.pyx`、`feed.pyx`、`observer.pyx`、`order.pyx`、`trade.pyx`、`position.pyx`、`comminfo.pyx`、`signal.pyx` 等：应用 `@cython.final`、局部 C 级缓存、`cdivision(True)`，尽量消除 dict/属性查找热路径。
  - 对 `cerebro.pyx` 的主循环进行审视，确保 runonce/preload 路径上无 Python 热点。

- **[阶段G] I/O 与数据源（并行推进，0.5周）**
  - 优先通用 CSV 与 Pandas 源：`feeds/csvgeneric.pyx`、`feeds/pandafeed.pyx`；配合 `utils/fast_strptime.pyx` 优化日期解析；避免不必要的对象创建与拷贝。

- **[阶段H] 并行化与可选 OpenMP（可选，0.5–1周）**
  - 在可独立循环段（如指标 `once`、统计聚合）尝试 `with nogil` 并行；如需 OpenMP，评估跨平台编译参数与可维护性再决定。

- **[阶段I] 内存优化专项（贯穿式）**
  - 统一以连续内存（memoryview/array）为主，减少 Python 容器；环形缓冲复用；避免临时切片；必要时引入对象池。

- **[阶段J] 回归验证与发布（0.5–1周）**
  - 跑 `pytest -n 8 tests/` 与基准；更新 `docs/` 与 `CHANGELOG.md`；打标记版本。


## 15. 详细优化清单（逐模块 TODO）

- **[全局与构建]**
  - [ ] 在 `setup.py` 增加 Debug/Release 开关策略，Release 对热点开启：`boundscheck(False)`、`wraparound(False)`、`nonecheck(False)`、`cdivision(True)`、`profile(False)`。
  - [ ] 校验分层构建顺序（Level 1–7）在三平台可用；必要时使用 `scripts/compile_in_batches.py` 分批构建。
  - [ ] 引入并使用 `pytest-xdist` 并行执行用例（与验收标准一致：`pytest -n 8 tests/`）。
  - [ ] 基准输出固化：保存 `benchmark_results.json` 与图表，记录日期与提交哈希。

- **[P0 核心路径]**
  - [ ] `cybacktrader/linebuffer.pyx`：
    - [ ] 用 `cdef inline` 实现索引计算与取值，消除 Python 取模；
    - [ ] 用 typed memoryview 承载底层数组，环形缓冲 O(1)；
    - [ ] Release 下关闭边界检查并回归；
    - [ ] 为读/写提供无GIL循环（纯计算段）。
  - [ ] `cybacktrader/lineseries.pyx`：
    - [ ] 将热点循环下沉为 C 级 for，统一 memoryview；
    - [ ] 关键 getter/setter 改为 `cdef inline`；
    - [ ] 若存在 Python 层切片/列表，替换为视图窗口；
    - [ ] 评估 `noexcept` 与 `@cython.final` 以减少虚调用。
  - [ ] `cybacktrader/functions.pyx`：
    - [ ] 统一使用 `libc.math`；
    - [ ] 高频小函数内联，必要处 `nogil`；
    - [ ] 消除 Python-level 异常路径对热函数的影响（必要时分离慢路径）。
  - [ ] `cybacktrader/dataseries.pyx`、`cybacktrader/lineiterator.pyx`：
    - [ ] 全面替换 Python 列表/属性访问为 C 级操作；
    - [ ] 复核 runonce/preload 路径，避免重复装箱与动态分派；
    - [ ] 增加微基准（每次提交后对比）。

- **[P1 指标体系]**
  - [ ] `cybacktrader/indicators/basicops.pyx`：
    - [ ] 滚动 O(n) 算法模板化（SMA/EMA/Highest/Lowest/SumN/MeanDev等）并内联；
    - [ ] `once` 中使用 memoryview + `with nogil`；
    - [ ] 统一输出写入为连续内存窗口，避免对象创建。
  - [ ] 高频指标专项（逐一验证 once 是否 C 级循环）：
    - [ ] `indicators/sma.pyx`
    - [ ] `indicators/ema.pyx`
    - [ ] `indicators/rsi.pyx`
    - [ ] `indicators/macd.pyx`
    - [ ] `indicators/bollinger.pyx`
    - [ ] `indicators/atr.pyx`
    - [ ] `indicators/stochastic.pyx`
    - [ ] `indicators/cci.pyx`
    - [ ] `indicators/momentum.pyx`
    - [ ] `indicators/williams.pyx`
  - [ ] 其他指标（以 5 类模式覆盖）：均复核 `once` 的向量化/内联程度与 GIL 管理。

- **[P2 调度撮合与外围]**
  - [ ] `cybacktrader/broker.pyx`：`@cython.final`、`cdivision(True)`、缓存 `getcommissioninfo` 等热路径结果。
  - [ ] `cybacktrader/feed.pyx`：日期转换与时区缓存，减少字符串解析；与 `utils/fast_strptime.pyx` 协作。
  - [ ] `cybacktrader/order.pyx`、`cybacktrader/trade.pyx`、`cybacktrader/position.pyx`、`cybacktrader/comminfo.pyx`：减少属性查找/字典访问，改为 C 级字段。
  - [ ] `cybacktrader/observer.pyx`：`@cython.final` 与 C 级成员，避免虚表开销。
  - [ ] `cybacktrader/cerebro.pyx`：主循环/任务调度的 Python 层调用摊销与缓存，确保 runonce 路径闭环在 C 级循环内完成。
  - [ ] `cybacktrader/strategy.pyx`、`cybacktrader/metabase.pyx`：若受元类限制，保持“薄封装+厚基础”的安全策略，避免侵入式 Cython 化。

- **[Feeds / I/O]**
  - [ ] `feeds/csvgeneric.pyx`、`feeds/pandafeed.pyx`：避免逐行 Python 处理，批量解析；日期解析走 `utils/fast_strptime.pyx`；必要时引入小型缓冲。
  - [ ] 其他数据源（`feeds/yahoo.pyx`、`feeds/ccxtfeed.pyx` 等）：以 I/O 为主，保证编译优化即可。

- **[Analyzers / Observers / Filters / Sizers / Plot]**
  - [ ] 这些多为非热点路径，保持已编译状态；仅在基准显示为瓶颈时再做定点优化。

- **[Utils]**
  - [ ] `utils/fast_strptime.pyx`、`utils/dateintern.pyx`：确保在 Feeds/核心路径被调用；移除重复的 Python 级解析。
  - [ ] `utils/autodict.pyx`、`utils/ordereddefaultdict.pyx`：核对是否仍在核心路径中被频繁访问，必要时替换为 C 级结构。

- **[脚本与自动化]**
  - [ ] `scripts/analyze_dependencies.py`：确认模块依赖有序性。
  - [ ] `scripts/check_compilation_status.py`、`scripts/monitor_compilation.py`：持续监控构建健康度。
  - [ ] `scripts/compile_in_batches.py`、`scripts/compile_remaining.py`：用于有问题模块的隔离与回归。
  - [ ] `scripts/analyze_failed_modules.py`：针对编译失败的模块产出诊断报告。

- **[结果固化与文档]**
  - [ ] 每轮优化后更新 `docs/` 与 `CHANGELOG.md`，记录性能数字、指令调整与风险点。
  - [ ] 在 `docs/` 增补“性能优化白皮书”：收录典型代码片段的前后对比与通用模式库。
